#!/usr/bin/node

// Use a for...of loop to go over each line in the array.
for (const line of ['C is fun', 'Python is cool', 'JavaScript is amazing']) {
  // To the console, print each line.
  console.log(line);
}
