#!/usr/bin/node

// Obtain the initial command-line parameter
const arg1 = process.argv[2];

// Obtain the second parameter on the command-line.
const arg2 = process.argv[3];

// Print the combined result in the format that has been provided.
console.log(`${arg1} is ${arg2}`);
