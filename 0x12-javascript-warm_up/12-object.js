#!/usr/bin/node

// Set up an object's properties, including their value and type.
const myObject = {
  type: 'object',
  value: 12
};

// Print the initial item.
console.log(myObject);

// Change myObject's value attribute to 89.
myObject.value = 89;

// Print the revised item.
console.log(myObject);
